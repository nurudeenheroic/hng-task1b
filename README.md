# hng-task1b
A simple profile card UI built as part of HNG Tech internship task 1B.



## Files

- `index.html` — main page structure.
- `index.css` — styling for the profile card and responsive layout.
- `index.js` —  handles epoch time update.

## Usage

1. Open `index.html` in your browser.

## Notes

1. The epoch tie is updated every 10ms;